SELECT * FROM users

-- INSERT INTO users (firstname, lastname, birthdate, email)
-- VALUES ("sherwin","montanez","jan 23 1994", "test@gmail.com");

-- CREATE TABLE users(
--   id INTEGER PRIMARY KEY AUTOINCREMENT,
--   firstname TEXT,
--   password TEXT,
--   email TEXT,
--   role TEXT DEFAULT 'User ',
--   created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
--   updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
-- )

-- SELECT * FROM emp

-- SELECT NAME from sqlite_master WHERE type='table'
-- SELECT * FROM users
